# E11 — Ordering & Fulfilment [PLANNED - NOT BUILT]
# Marketplace

**Status:** baselined - brownfield retrofit; E11-S01 `built 2026-08-13` — **the record is built, the context is not**
**Version:** 1.3
**Date:** 2026-08-14
**Author:** epics-agent
**Bounded context:** BC-11 — Ordering & Fulfilment [PLANNED - NOT BUILT]
**Changelog:** v1.0 - initial retrofit; reverse-engineered from the 15-repo working tree.
v1.1 - 2026-08-13: **E11-S01 is `built`**, which means its two criteria were checked against the working
tree rather than assumed — and both found something. BC-11 was quoting `item.js` with a sentence that file
does not contain, and §3's schemas listing was three entries out of date; neither changed a claim, both were
repaired. Question 4 of §6 stays despite failing criterion 2's "traceable upstream" test, with the reason
written beside it: it is a tier-topology question, not a commerce design, and deleting it would hide a real
blocker. Question 5 is marked **narrowed, not answered** — E12 gave this platform a retention decision and a
privacy notice, neither of which reaches what an order would store. Nothing was designed here and nothing may
be: no collection, no migration, no resolver, no price.
v1.2 - 2026-08-14: §6 question 3 is **narrowed, not answered**, and for the second time this file records a
question outliving the upstream one it was carried from. The platform owner accepted the `item.published`
race — last writer wins, no lock field (`phase2/BOUNDED_CONTEXT.md` §7 q5, closed) — which settles the
concurrency half and leaves the BC-11 half untouched: whether an order snapshots the catalogue state it was
placed against, rather than referencing a flag either writer can flip afterwards. Nothing designed here
either.
v1.3 - 2026-08-14: publishing was split out of `itemUpdate` and `companyUpdate` on both tiers the same day
(`phase5/CATALOGUE.md` E05-S08, `phase5/COMPANY_LEGAL_ENTITY.md` E04-S08), so `published` now has exactly one writer per tier instead
of riding inside every save. Question 3 is **unchanged by that** and says so: a dedicated writer makes the
flip deliberate, not slower — an order holding a live reference still sees whatever the last deliberate
publisher left. Recorded so the split is not later mistaken for an answer. Nothing designed here either.

## 1. Epic goal

Record the gap, not close it. This epic names Cart, Order, Delivery, Payment for glossary and boundary
readiness (`phase2/BOUNDED_CONTEXT.md:190`) and lists what blocks starting design. It ships no
collection, no migration, no model, no resolver, no schema builder, no state machine, no sequence
diagram of a checkout flow. Per `phase5/CONSTRAINTS.md` §6: "a risk-register row naming the gap is fine;
a story designing the fix is not."

## 2. Scope

| In scope | Out of scope | Why |
|---|---|---|
| Naming the gap and its blocking questions | A cart schema or mutation | No design exists; inventing one violates [`CLAUDE.md`](../../../../CLAUDE.md) §Build state "ask before inventing them" |
| Citing what `item` already lacks because of this gap | An order state machine or resolver signature | `phase5/CONSTRAINTS.md` §6 — "no story defines a state machine, no story defines a resolver signature" |
| Recording the open questions that block design start | A delivery-cost model | No design exists; naming one is designing one |
| — | A payment gateway or error taxonomy | No gateway chosen, no integration story permitted |

## 3. Build state

**Not built.** Verified empirically this pass, not assumed:

- `find BEs/dev -path "*/mutations/*" -iregex '.*\(cart\|order\|payment\|delivery\).*'` — zero matches
  across all 9 backend services under `BEs/dev/`.
- No collection named `cart`, `order`, `delivery`, or `payment` in `BEs/marketplace-db-setup/lib/schemas/`.
  ⚠️ **Re-listed 2026-08-13 (E11-S01)** — the directory holds eleven entries, not the eight this bullet
  used to name: `account.js`, `admin.js`, `collection.js`, `company.js`, `encrypted.js`, `geo.js`,
  `item.js`, `itemCategory.js`, `shopOwner.js`, `user.js` and a `README.md`. `admin.js` and `encrypted.js`
  were the two missing; none of the three is commerce, so the claim this bullet makes is unchanged and only
  its evidence was stale.
- `item` deliberately carries no `price` field — `BEs/marketplace-db-setup/lib/schemas/item.js`:
  "There is no `price`. Cart, order, delivery and payment have no model anywhere on this platform and no
  design decision behind them yet, so a price would be a guess at a currency, a precision, a VAT
  treatment and a discount model all at once."
- Delivery has no collection, no resolver, and no design anywhere on this platform — ADR-008. No
  successor exists.
- `phase2/UBIQUITOUS_LANGUAGE.md` §18 "Planned commerce vocabulary — NOT BUILT" lists Cart, Order,
  Delivery, Payment each as `NOT BUILT`, named for glossary readiness only.
- `phase4/API_CONTRACTS.md:525`: "Operations that do not exist and are not designed: anything
  ordering-related — cart, order, delivery, payment. No collection, no resolver, no schema slice, no
  ADR." (cited as `:516-518` until 2026-08-13; the file grew above it, the sentence did not move.)

## 4. Stories

This context gets exactly one story, and it is a recording story — not a design story
(`phase5/CONSTRAINTS.md` §6).

### E11-S01 — Record the commerce gap and its blocking questions   `built 2026-08-13`
Technical story. Keep the platform's one authoritative record of what Ordering & Fulfilment would need
before design can start, so no future agent infers a shape from the other 10 contexts and builds ahead
of a decision nobody made.
**domains:** documentation
**Acceptance criteria:**
- `phase2/BOUNDED_CONTEXT.md` §BC-11 and this file agree on the same 4 named concepts (Cart, Order,
  Delivery, Payment) — no 5th concept invented, no concept dropped.
- Every blocking question below is traceable to a real open question in an upstream Phase 1/2 document,
  not invented in this file.
**Traces:** BCON entries n/a (no gate applies to undesigned code); `phase5/CONSTRAINTS.md` §6.
**Evidence:** absence itself — see §3 above for the grep/file-listing that proves it.

**Built 2026-08-13.** Nothing was designed and nothing was invented — the story is the record, and building
it means re-running the checks it rests on and repairing what had drifted. **Criterion 1 holds**: BC-11
(`phase2/BOUNDED_CONTEXT.md:217-222`) and this file name the same four concepts, Cart / Order / Delivery /
Payment, with no fifth and none dropped. One defect was found on that comparison and fixed: BC-11 quoted
`item.js` as saying a price "would be a guess at a design decision nobody has made", which is not a string in
that file — it now quotes `item.js:11-13` exactly. §3's evidence was re-run rather than trusted: zero
commerce mutations across the nine services, `item` still has no `price`, and the schemas directory listing
was stale by three entries, none of them commerce.

⚠️ **Criterion 2 holds for four of the five questions, and question 4 is the exception — deliberately kept.**
Questions 1 and 3 trace to `phase2/BOUNDED_CONTEXT.md` §7 open questions 4 and 5, question 2 to `item.js`'s
own comment, question 5 to `phase1/NFR.md` open question 1. Question 4 — whether order-adjacent operator work
needs a tier of its own — says outright that it is raised nowhere upstream, and it stays: deleting it to
satisfy the criterion would remove a real blocker, and it is a question about tier topology (ADR-002,
`phase2/UBIQUITOUS_LANGUAGE.md` §Service pair) rather than a commerce design decision, so recording it
invents no shape. The criterion is what caught it, which is the criterion working.

**What `built` means here, since the epic's title says NOT BUILT and still does:** the record is current and
verified, not that Ordering & Fulfilment exists. No collection, no migration, no model, no resolver, no state
machine and no price were added, and none may be until §6 is answered by product and the platform owner.

## 5. Dependencies

- **Would consume, once built:** BC-05 Catalogue's `item` (still priceless — `BEs/marketplace-db-setup/lib/schemas/item.js`),
  BC-07 Customer Account's `addresses` (for delivery — `BEs/marketplace-db-setup/lib/schemas/user.js`),
  BC-04 Legal Entity/Company (for fulfilment ownership) — `phase2/BOUNDED_CONTEXT.md:192`.
- **Would need E10's shared kernel** the same way every other context does — a 7th Mongoose model, a
  new `TIER`-scoped service pair (`phase2/UBIQUITOUS_LANGUAGE.md` "Service pair": "A fifth tier means a
  fifth service pair, not a role check bolted onto the existing ones" — here it would be a new *concern*
  inside an existing tier, or a genuinely new collection-owning context; which one is open question 4
  below).
- **Landing order:** none — nothing lands until the open questions below are answered by product +
  platform dev. No repo, no branch, no migration exists to sequence.

## 6. Open questions

Carried verbatim from `phase2/BOUNDED_CONTEXT.md` §7 open question 4 and `phase5/CONSTRAINTS.md` §6 —
not invented here:

| # | Question | Owner |
|---|---|---|
| 1 | When BC-11 design work starts, who signs off the first schema — and does it become one context or split (Cart / Order / Delivery / Payment each their own)? | Product + platform dev (`phase2/BOUNDED_CONTEXT.md` open question 4) |
| 2 | What currency, precision, VAT treatment and discount model does a `price` on `item` need, given none of the 4 has been decided? | Product (`BEs/marketplace-db-setup/lib/schemas/item.js`) |
| 3 | Does `item.published` need a version/lock field before ordering can safely reference a specific catalogue state at time-of-order? ⚠️ **Narrowed 2026-08-14, not answered.** The upstream question it was carried from — `phase2/BOUNDED_CONTEXT.md` §7 q5, the two-writer race — is **closed**: the platform owner accepted last-writer-wins on `item.published` and no lock field is coming. What survives here is the half BC-11 owns and nobody has asked: an order that references a live `published` flag references a value either writer can flip afterwards, so whether an order snapshots the catalogue state it was placed against is still a design decision. ⚠️ **The publish split of the same day does not answer this either** — `itemUpdatePublished` makes every flip deliberate, which removes the accidental republish but not the flip; an order referencing the live flag still reads whatever the last deliberate publisher left | Platform dev (`phase2/BOUNDED_CONTEXT.md` open question 5 — raised for BC-05, directly blocks any Order design) |
| 4 | Is a new tier needed for order-adjacent operator work, or does Ordering & Fulfilment stay inside the existing `User`/`ShopOwner`/`Admin` tiers as new resolvers on existing resource services? | Product + platform dev — not raised anywhere upstream, flagged here because it blocks even a first schema draft |
| 5 | Does GDPR/data-protection scope (`phase1/NFR.md` NFR-CO02, open question 1) need resolving before an order stores payment-adjacent PII? ⚠️ **Narrowed 2026-08-11, not answered**: the owner decided edge-log retention and E12-S25 published this platform's first privacy notice, so the question is no longer "nothing exists" — it is still open for anything an order would store | Platform owner (`phase1/NFR.md` open question 1, `phase1/NFR.md` changelog v1.2) |
