# E15 — Session Index, Account Revocation & Credential Teardown
# Marketplace

**Status:** proposed - audit remediation, not baselined; E15-S01 and E15-S09 built 2026-08-12, E15-S08, E15-S02 and E15-S03 2026-08-13
**Version:** 1.8
**Date:** 2026-08-13
**Author:** epics-agent
**Bounded context:** hardens BC-01 — Identity & Access, BC-02 — Session Termination and BC-03 — Shop Owner Onboarding & Approval; implemented largely inside BC-10 — Shared Kernel. Introduces no new context.
**Source:** [`docs/report/token-handling-security-audit.md`](../../../report/token-handling-security-audit.md) v1.1 §3.3, §3.6b, §3.6e
**Changelog:** v1.2 - pruning question answered: `HEXPIRE` with the field TTL set to the session's absolute cap, decision carried by E15-S03; Valkey recorded as an unsupported target. v1.1 - open question 1 answered: a password change revokes the calling session too. Decision carried by E15-S05 and removed from §6; E15-S06 inherits it. The Redis-version half of the pruning question closed by the 7.4 floor now recorded in `docker-DBs/README.md` §Redis. Three wrong `logout.mts` paths corrected (`graphQL/` → `graphQLApi/`). v1.0 - initial; written against the 2026-08-10 token-handling security audit, after an adversarial review of the underlying design.
v1.3 - 2026-08-11: the non-1:1 bounded-context caveat removed. `phase5/CONSTRAINTS.md` §5 no longer
ties an epic to a bounded context, so this epic breaks no rule. That §5 is the record of the decision.
v1.4 - 2026-08-12: **E15-S01 is `built`, and the live defect is closed** — `logout` deletes both tokens
instead of answering success and deleting nothing. The four-character fix was the small half. The large
half is that **the test suite had been agreeing with the defect**: every seed in the service, over a real
Redis cluster, wrote the field the reader asked for, so twenty-six integration tests certified a logout
that logged nobody out. Seeds now come from typed `IRefreshData` / `IRedisDataUser` fixtures in
`test/helpers/sessionFixtures.mts`, shared by all three suites, and reverting the fix now fails 11 tests
instead of none. Two of the story's five criteria turned out to be already satisfied by E13-S02 and
E14-S01, which landed after the story was written, so it touched one repo rather than two. Three documents
were describing the broken behaviour as the intended one and are corrected: ADR-005 quoted the wrong field
as evidence for a decision that never depended on it, `phase2/EVENT_STORMING.md` quoted the pre-E13 code,
and `phase5/SEQUENCE_DIAGRAMS.md` §5 omitted the session lookup entirely — which is part of why the defect
was invisible to review. E15 reads 1 of 9.
v1.5 - 2026-08-12: **E15-S09 is `built`**, closing the first landing-order repo. The resolver's context was
typed `IContextLogout`, which declares `state.user` as always present; the `x-introspectioncode` bypass
reaches the resolver without ever assigning it. The type is widened locally — the shared one is correct for
the services whose middleware does fill it — and the optional chain, the non-null assertion and the Stryker
exclusion that stood in for a test are gone with it. The no-session path is now an ordinary tested branch
that stops filing a Sentry event for a teardown it never attempted. One mutation exclusion removed, none
added, over five more live mutants. E15 reads 2 of 9, and
`marketplace-dev-authenticated-logout` is done until step 5 of §5's landing order.
v1.6 - 2026-08-13: **both remaining platform-owner questions answered, and E15-S08 is `built` on the
strength of one of them.** An index entry carries `tier` and `mintedAt` and nothing else; `familyId` and the
cap were offered and refused as duplicated E14 state. On `waitApprov` the answer was to enforce — and the
gate turned out to have been built already, by E01-S11 on 2026-08-12, from the other side of the same
problem: E03-S08 opened self-registration, and a public form that mints `waitApprov: true` is only safe if
something refuses it. **The document still claiming the enforcement was missing was this epic**, §2 and §3,
not `SECURITY_AUTH.md`, which had been corrected when the gate landed. Both are fixed above. The audit
report gains a header saying what it is — a dated artefact whose findings are closed by epics and never
edited — because an un-annotated finding there had started to read as an open one. E15 reads 3 of 9.
v1.7 - 2026-08-13: **E15-S02 is `built`** — the index this whole epic is named for now exists, and an
account can be enumerated without touching the keyspace. One helper in `marketplace-common` holds the key
shape, the field name and the TTL, and the two writers differ only in *where* they call it: rotation
before the family write, so a failure rolls back while the old refresh token still works; login last in
its try, because a session nothing can list is a session nothing can revoke. The value is the `tier` and
`mintedAt` the platform owner agreed to and nothing else, with `mintedAt` the lineage's `originalLogin` so
a session that refreshes every fifteen minutes is not reported as fifteen minutes old. One residual is
recorded rather than hidden: revoking a refresh token leaves the access token minted from it working until
its own expiry — the same residual `disabled` already carries. `docs/data-model.md` gains the key shape and
the four properties of it that are load-bearing. E15 reads 4 of 9, and landing-order steps 3 and 6 are
done.
v1.8 - 2026-08-13: **E15-S03 is `built`** — the index no longer only grows. Rotation and logout unfile the
token they delete, and every field the index gains carries an `HEXPIRE` of what remains of its own
session's cap: a session that ends without passing through either takes its row with it, so the stale-field
bound is zero by design rather than small. The field TTL and the cap refusal read one expression,
`sessionCapDeadline`, because two copies of that arithmetic fail silently in both directions. Rotation
inherits the predecessor's remainder rather than minting a fresh cap, which is what stops a client that
refreshes for ever from being listed for ever. **The 7.4 floor is now checked twice instead of assumed** —
`up.sh --with-redis` reads the container's version, and each of the four authorization services probes its
own connection with an `HTTL` at boot, because Redis refuses an unknown command at the first call rather
than at startup. One thing found rather than built: the three rotation suites had been tolerating an
unasserted index write since E15-S02, and now assert all of it. E15 reads 5 of 9.

## 1. Epic goal

Make an account able to reach its own sessions. Today nothing in this platform can answer "which sessions
does this account have open?", so nothing can end them: changing a password leaves every stolen session
alive, disabling a ShopOwner leaves them logged in, and `logout` — the one deliberate teardown that exists —
was a silent no-op in production because it read a field name that was never written. This epic fixed the
broken logout first (E15-S01, 2026-08-12), then builds the account→sessions index that every other teardown
needs, then wires the credential and account-state writes to use it.

## 2. Scope

| Item | In/Out | Why |
|---|---|---|
| The `logout` field-name defect | In | live production defect found while auditing §3.3; nothing downstream is worth building on a logout that never fires |
| `${REDIS_KEY}idx:<tier>:<accountId>` account→sessions index | In | closes audit §3.6b; every story after S02 needs it |
| `revokeAllSessionsForAccount` in `marketplace-common` | In | one routine, four callers, BCON-08-shaped |
| Password change → revoke all sessions | In | closes audit §3.3, the report's stated first priority |
| Email change → revoke all sessions | In | same finding; a changed login identifier is a credential write |
| ShopOwner status transitions → revoke | In | closes audit §3.6e's live half — a disabled owner keeps working today |
| Enforcing `waitApprov` at the session boundary | ~~In~~ **Closed elsewhere** | was audit §3.6e's doc half; E01-S11 built the gate on 2026-08-12 and `SECURITY_AUTH.md:80` now describes it accurately — E15-S08 verifies rather than builds |
| An operator-facing "log out this user" | Out | E17 — same routine, different trigger, different repo |
| Family revocation on token reuse | Out | E14 — different key, different trigger |
| Hashing the session key namespace | Out | E13, but this epic **depends** on it — see §5 |

## 3. Build state

**Not built, and one part was actively broken until E15-S01 fixed it.**

- ~~**`logout` never deletes anything.**~~ **Fixed 2026-08-12 by E15-S01.** As found:
  `BEs/dev/marketplace-dev-authenticated-logout/src/lib/authorizationLogoutHandler.mts:60` read
  `redisClient.hGet(\`${REDIS_KEY}${refreshToken}\`, 'id')`. Nothing ever wrote an `id` field: the hash is
  `IRefreshData` per `BEs/marketplace-common/src/others/IRefreshData.mts`, written as `_id` by
  `refreshSessionTokens.mts` and by all three login writers. `hGet` therefore returned `null`, the handler
  took its "no session" branch, and the deletes in
  `BEs/dev/marketplace-dev-authenticated-logout/src/graphQLApi/schema/mutations/logout.mts` never executed.
  Every logout on this platform returned success and left both tokens live until natural expiry.
  The read is now `_id`, and the field name is asserted against a writer-shaped hash — see the story.
- **No account→sessions index of any kind.** `grep -rn 'idx:' BEs/` returns nothing; the only Redis key
  shapes in the workspace are the bare token keys.
- **No credential write touches Redis.** The password and email mutations under
  `BEs/dev/marketplace-dev-authenticated-resource/src/` and their Admin and User siblings write to MongoDB and
  return. None of them imports `redisClient`.
- ~~**`waitApprov` is enforced nowhere.**~~ **No longer true, and it was not this epic that changed it.**
  As found, the flag was written and displayed and read by nothing, while `phase3/SECURITY_AUTH.md` listed
  it as a control. **E01-S11 built the gate on 2026-08-12**: `checkShopOwnerApproval` in
  `BEs/marketplace-common/src/others/checkShopOwnerApproval.mts` is called by `tryLoginShopOwner` (4028) so a
  parked account cannot start a session, and by `tokenInfoShopOwner` (4029) so parking one ends the session it
  already holds within one access-token lifetime. `SECURITY_AUTH.md:80` now describes exactly that. E15-S08
  therefore closes on branch (a) without this epic writing a line of it — see the story.
- Precedent that this is architecturally allowed: a `*-resource` service already reaches Redis from a
  mutation path — `BEs/dev/marketplace-dev-public-resource/src/lib/access/guardPublicWrite.mts:1,46-47`
  imports `redisClient` and calls `assertUnderRateLimit`. Nothing new is being invented here.

## 4. Stories

### E15-S01 — `logout` actually logs out   `built 2026-08-12`
**As a** customer clicking "log out", **when** the mutation returns success, **I want** my two tokens gone
from Redis **so that** the button does what it says.
**domains:** backend, testing
**Acceptance criteria:**
- `authorizationLogoutHandler.mts:60` reads `'_id'`; a unit test asserts the `hGet` call arguments exactly,
  so the field name is pinned rather than incidentally satisfied
- Both `del` calls in `logout.mts:17,23` are exercised by a test that seeds a hash containing `_id` and
  asserts each `del` was called once with its exact key — the current suite proves only the null branch
- `test/integration/index.itest.mts:147-174` is rewritten to assert the tokens are **absent** from Redis after
  the call, not merely that the response was 204; the previous assertion is removed, not extended
- All login writers store `_id` as a string via `_id.toString()`, at every call site, so `hGet` returns a
  usable identifier rather than a stringified `ObjectId` in one path and a plain string in another; asserted
  with `toStrictEqual` on the written hash in each writer's test
- `yarn test:cov` 100/4 and `yarn test:mutation` 100 in `marketplace-dev-authenticated-logout`
**Traces:** NFR-SE01, NFR-SE03, NFR-SE05, NFR-MA01, NFR-MA02; ADR-005; BCON-01, BCON-02, BCON-09
**Evidence (defect):** `BEs/dev/marketplace-dev-authenticated-logout/src/lib/authorizationLogoutHandler.mts:60`

> This is a live defect, not a hardening. It ranks first in this epic and should ship on its own branch ahead
> of everything else here — it is a four-character fix plus honest tests, and every later story assumes a
> logout that works.

**Evidence (built):** the fix is the four characters the story predicted —
`readSessionField(redisClient, refreshToken, 'id')` → `'_id'` at
`src/lib/authorizationLogoutHandler.mts:92`. Everything else in the change is the "honest tests" half, and
it is the larger half for a reason worth recording.

⚠️ **The suite was not silent about this defect — it agreed with it.** Every seed in this service, unit and
integration alike, wrote the field the reader asked for: `hSet(refreshKey, 'id', 'itest')`. Twenty-six
integration tests ran against a live Redis cluster and reported a working logout, because the only thing
they proved was that the handler could find a hash the test had shaped to its own question. The fix is
therefore paired with `test/helpers/sessionFixtures.mts`: one typed `IRefreshData` and one typed
`IRedisDataUser`, seeded whole, shared by the unit suite and both integration suites. A reader asking for a
field no writer writes now misses — and reverting the four characters proves it rather than illustrating
it: **7 integration tests and 4 unit tests fail**, where the old seeds failed none.

Two assertions were added on top of the changed seeds: a unit test that drives `hGet` out of the written
hashes and pins both reads to `_id`, and its inverse — the real hash **minus** `_id`, which `exists`
answers 1 for and `hGetAll` returns four populated fields for, still correctly refused with
`throwAlreadyDone`. The integration suite's "hash exists but lacks the field" case was rebuilt the same
way, from `IRefreshData` minus its identity rather than from an invented field name.

⚠️ **Two criteria were already met before the story started, by work that landed after it was written.**
The integration suite asserts absence from Redis with `exists(...) === 0` rather than a bare 204 (E13-S02),
and all three login writers already store `_id: id.toString()` at every call site, asserted whole against a
real `Types.ObjectId` in `login.test.mts`, `loginAdmin.test.mts` and `loginUser.test.mts` (E14-S01). The
story's line references are pre-E13 throughout — `logout.mts:17,23` are now one `deleteSession` call per
token, each deleting both key shapes. Nothing in `marketplace-dev-public-authorization` needed to change,
so this story lands in **one repo**.

**Gates:** `yarn test:cov` 100% on all four metrics (126 tests, 13 files), `yarn test:mutation` 100 (102
mutants killed, 0 survived) in `marketplace-dev-authenticated-logout`.

### E15-S02 — Every login and rotation records the session under its account   `built 2026-08-13`
State plainly: an account must be able to enumerate its own live sessions without scanning the keyspace.
**domains:** backend, testing
**Acceptance criteria:**
- Login and rotation both `hSet` one field into `${REDIS_KEY}idx:<tier>:<accountId>`, whose **field name** is
  the hashed session identifier produced by E13's `hashSessionToken` and whose value carries no token
  material; a unit test asserts the field name matches `/^[0-9a-f]{64}$/` and that the value's key set is
  exactly the agreed non-secret set
- The index key itself is `expire`d to `SESSION_CAP_DAYS_REMEMBERED` (E14-S05) on every write — the **longer**
  of the two caps, unconditionally, whatever cap the session being written carries. An abandoned index then
  cannot outlive every session it names, and a 30-day remembered session cannot outlive the index that names
  it. Using the session's own cap would be tighter and wrong: one 1-day login after a 30-day one would pull
  the whole key's TTL down to a day and orphan the remembered session — a live session listed nowhere, which
  E15-S04's revocation would then silently miss. E15-S03 prunes fields that outlive their sessions; nothing
  in this epic recovers a session that outlives its index entry, so the TTL must never be the shorter number
- `SCAN`, `KEYS` and any glob-matching command appear nowhere in the diff — `grep -rniE '\b(scan|keys)\(' `
  over the change returns zero hits (BCON-08: a Redis cluster makes keyspace scanning both wrong and slow)
- One unit test per writer asserts the exact `hSet` and `expire` arguments; Stryker 100 in every touched
  package with no threshold change
**Traces:** NFR-SE01, NFR-SE05, NFR-SC02, NFR-SC03, NFR-MA01, NFR-MA02; ADR-003, ADR-004; BCON-01, BCON-08
**Evidence (gap):** no `idx:` key shape exists anywhere in `BEs/`

**Evidence (built):** one helper, two writers, and the whole contract sits in the helper —
`indexSession(store, prefixedRefreshToken, refreshTokenData)` in
`marketplace-common/src/others/sessionKeys.mts`, beside `sessionKey` and `hashSessionToken`, so no call
site builds an index key or a field name out of a template literal. It takes the whole `IRefreshData`
rather than a tier and an id: tier, account id and mint time then come from one object and no caller can
pair a tier with a stranger's id.

The value is `{ tier, mintedAt }`, decided by the platform owner on 2026-08-13, and **`mintedAt` is the
lineage's `originalLogin`, not the current token's mint time** — rotation carries it forward unchanged, so
a session refreshing every fifteen minutes reads as a login from when the customer actually logged in.

⚠️ **The two writers order the index write differently, and both orders are deliberate.** In rotation
(`refreshSessionTokens.mts`) it sits after the pair's two `expire`s and before the family `sAdd`, so a
failed index write rolls back while the **old refresh token is still alive** — the client keeps a working
session instead of one whose successor was never recorded. In login (`setRedisLoginSession.mts`) it is
last inside the `try` and its failure fails the login: a session that cannot be listed is a session
E15-S04 can never revoke, so a half-made login is worse than none. That rollback deletes the two session
keys and *not* the index field — what it leaves is a row naming keys that no longer exist, a stale listing
and never a credential, and E15-S03's per-field TTL is what removes it.

**The TTL is the literal 2 592 000 in every test, never the constant.** `SESSION_INDEX_TTL_SECONDS` is
derived from `SESSION_CAP_DAYS_REMEMBERED × MILLISECONDS_PER_DAY / 1000`; a test importing it would agree
with the constant about any value, including a mutated one. The login fixture carries
`sessionCapDays: '1'` precisely so the assertion proves the key still gets thirty days.

BCON-08 verified directly: `grep -rniE '\b(scan|keys)\('` over both diffs returns only `Object.keys` — a
JS built-in, in tests. No Redis keyspace-scanning command appears anywhere in the change.

**Gates:** `marketplace-common` — 617 tests, coverage 100% on all four metrics (622/622, 274/274, 121/121,
577/577), `yarn test:mutation` **100.00** with `sessionKeys.mts` at 51 mutants and `refreshSessionTokens.mts`
at 16, zero survivors; `./deploy-local.sh` run, 12 consumers.
`marketplace-dev-public-authorization` — `tsc --noEmit` clean, 210 unit tests, coverage 100% (216/216,
42/42, 44/44, 212/212), `yarn test:mutation` **100.00**, `yarn test:integration` 6 files / 41 tests against
the live Redis cluster. The integration suite is the only place the contract can actually be proved: it
rebuilds the session key from the index field and reads back the same hash the login wrote.

### E15-S03 — The index prunes itself   `built 2026-08-13`
State plainly: an index that only ever grows becomes both a memory leak and a lie about which sessions are
live — pruning is part of the feature, not a follow-up.
**domains:** backend, testing
**Acceptance criteria:**
- Rotation removes the superseded field with `hDel` in the same routine that adds the successor
  (`refreshSessionTokens`), and `logout` removes its own field; both asserted with exact arguments
- Fields whose sessions expired without passing through rotation or logout are removed by **`HEXPIRE`, a
  per-field TTL** — decided 2026-08-10, see below. Every `hSet` into the index is followed by an `HEXPIRE`
  on that field whose value is the **remaining time to `originalLogin + sessionCapDays`** (E14-S05), not the
  session key's own Redis TTL and not the index key's; both unit tests assert the two commands and their
  exact arguments
- **Rotation does not reset the field TTL.** The cap is absolute from the original login, so the successor
  field is written with the remaining cap the predecessor carried, computed from the same `originalLogin`. A
  test rotates a session close to its cap and asserts the successor field's `HPTTL` is the remainder rather
  than a fresh full cap — otherwise a rotating session is listed as live for ever
- **Upper bound on stale fields per account: zero by design** — a field cannot outlive the session it names
  by more than Redis's own expiry latency, because the TTL is the cap itself. Demonstrated by an integration
  test that writes a field with a one-second TTL, waits, and asserts both `hKeys` and `HTTL` no longer see it;
  a second assertion proves a live field's `HTTL` equals the cap remainder
- `HEXPIRE` is a single-key command and the index key is one key, so BCON-08 holds; the `hSet` and the
  `HEXPIRE` address the same key and may share one pipeline. No command in the diff takes more than one key —
  asserted the same way E15-S02 asserts the absence of `SCAN` and `KEYS`
- The Redis floor this requires — **7.4.0 or newer, Redis proper** — is recorded in
  [`docker-DBs/README.md`](../../../../docker-DBs/README.md) §Redis and pinned by `docker-DBs/env`. The story
  still carries a startup version check that fails loudly rather than assuming the floor holds
- The chosen mechanism is written into `docs/data-model.md` alongside the other Redis key shapes, in the same
  commit — no undocumented key shape survives this epic (BCON-03)
**Traces:** NFR-SE05, NFR-PE02, NFR-SC02, NFR-MA01, NFR-MA02, NFR-MA05; BCON-01, BCON-03, BCON-08
**Evidence (gap):** none — the index does not exist yet; this criterion exists so it is not built without it

> **Decided 2026-08-10: `HEXPIRE`, not a lazy prune.** The two are not interchangeable implementations of one
> idea, which is what the choice looked like when it was written down. **A lazy prune asks the wrong
> question.** Its liveness test is *does the session key still exist*, and after E14-S05 a session key can
> exist while the session it holds is already dead: the key carries the sliding 90-day `REFRESH_TOKEN_EXPIRY`
> from `@axiumine/koa-utils`, but the session is refused at its absolute cap of one day, or thirty when
> remembered. A prune built on key existence therefore keeps naming sessions nobody can use — and E17 renders
> that list to an operator, who would be looking at logins that cannot log in. A field TTL set to the cap
> expires the entry when the session stops working, which is the property actually wanted, enforced by Redis
> rather than by a read path remembering to run.
>
> **The rest follows the same way.** Lazy prune costs `hKeys` plus one `EXISTS` per field plus an `hDel` per
> corpse on *every* read — every revoke and every console render — against one extra single-key write command
> per login. Its stale-field bound is not zero but "logins in thirty days" for an account that never revokes
> and never opens the console, since nothing prunes an index nobody reads. And it is the larger test surface:
> a branch, a dangling-field fixture, and a proof that its cleanup never batches into a multi-key `del`.
>
> **What it costs, recorded so a later reversal is a decision rather than a discovery.** The platform can
> never run on a Redis without hash-field TTLs. That is free today — dev-only, self-hosted, pinned at
> `7.4-alpine`, floor written into `docker-DBs/README.md`. **Valkey is explicitly not a supported target**
> (decided 2026-08-10), so its command coverage is not a constraint on this choice and no compatibility layer
> is owed to it. If some future deployment genuinely cannot offer hash-field TTLs, the lazy prune returns as
> a code change and not a config flag — because of the predicate difference above, the two mechanisms are not
> drop-in for one another.

**Evidence (built):** the field TTL and the cap refusal are **one expression, read by two callers** —
`sessionCapDeadline(originalLogin, sessionCapDays)` in `marketplace-common/src/others/sessionLifetime.mts`,
where `resolveAuthorizationSession` already computed the instant a session stops being honoured. Two copies
of that arithmetic could drift by a rounding, and both directions fail silently: a row outliving its session
is a listing an operator cannot act on, a row dying first is a live session listed nowhere. Its second half,
`sessionCapRemainingSeconds`, **rounds up and floors at one second** — early expiry is the orphan E15-S02's
key TTL exists to prevent, and Redis reads a non-positive hash-field TTL as "delete now", which is right for
a session already past its cap but the wrong shape for a caller to depend on.

`indexSession` now writes three commands in one order — `hSet`, then `expire(key, 2_592_000)`, then
`hExpire(key, field, remaining)` — and **the two TTLs are deliberately different numbers on the same write**:
thirty days for the key, whatever is left of *this* session's cap for the field. `unindexSession` is the
mirror, and rotation calls it **after** `deleteSession`, never before: the other order leaves a usable
refresh token listed nowhere for as long as the delete takes. Every rotation suite asserts that order with
`invocationCallOrder`, not by reading the source.

**Rotation does not reset the field TTL** because the successor is written from the predecessor's
`originalLogin`, which E15-S02 already carried forward. Nothing new enforces it; the tests pin a clock an
hour after login and assert `30 * 86_400 - 3_600` as a literal, so a mutant that swaps `originalLogin` for
`Date.now()` dies.

⚠️ **`assertHashFieldTTLSupport` exists because Redis refuses an unknown command at the first call, not at
startup.** Without it a service boots on a 7.2 server, verifies every token it is handed, and dies inside
the first login of the day with the cause three layers below the symptom. It probes with `hTTL` — read-only,
single-key — on the connection the service will actually use, before anything else touches it, and the four
authorization services assert exactly that with `invocationCallOrder` against `loadKeygrip`. The logout
service does not carry it: `hDel` has existed since Redis 2.0. `up.sh --with-redis` checks the container's
version independently, because the probe only sees whatever `REDIS_URL` points at.

**The three rotation suites had been agreeing with a write they never asserted.** Since E15-S02 their mocked
`redisClient` happened to expose `hSet` and `expire`, so `indexSession`'s write passed through unremarked;
adding `hExpire` turned that silence into four hard failures. The fix asserts the whole index write — key,
field name, JSON value, count raised to four — rather than adding the missing mock and moving on.

Live proof rather than a mocked one: the integration suite in `marketplace-dev-public-authorization` writes
a field with a one-second TTL against the real cluster, waits, and asserts `hKeys` and `HTTL` both stop
seeing it, then asserts a live field's `HTTL` is the cap remainder. BCON-08 holds by inspection —
`hExpire`, `hTTL` and `hDel` each take one key, and the diff adds no multi-key command.

**Gates:** all six repos green before their commits — coverage 100% on all four metrics and
`yarn test:mutation` **100.00**, no threshold touched, no Stryker exclusion added.
`marketplace-common` `4892f65` (`./deploy-local.sh`, 12 consumers) → `marketplace-dev-authenticated-logout`
`14074ef` → `marketplace-dev-public-authorization` `e904792` (213 unit tests, 232 mutants, integration run
against the live Redis) → `marketplace-dev-authenticated-authorization` `974e98e` →
`marketplace-dev-admin-authenticated-authorization` `8434e0d` →
`marketplace-dev-user-authenticated-authorization` `534a441` — the landing order of §5, unchanged.
`docker-DBs/up.sh` verified against the running container: `7.4.10` accepted, `7.2.5` and `6.2.14` refused,
`8.0.1` accepted — `sort -V`, so `7.10.0` does not read as older than the floor.

### E15-S04 — One routine revokes every session an account holds   `not built`
**As a** platform, **when** any code decides an account's sessions must end, **I want** exactly one tested
routine to call **so that** four callers cannot each get the Redis command shape subtly wrong.
**domains:** backend, testing
**Acceptance criteria:**
- `revokeAllSessionsForAccount({ tier, accountId })` lives in `marketplace-common`, is exported from
  `package.json`, reads the index with `hKeys`, issues **one single-key `del` per session key** and deletes
  the index key last; the unit test asserts one `del` per member with exact arguments and asserts no `del`
  ever receives two keys (BCON-08)
- A revoke over an empty or missing index completes without throwing and issues no `del` — tested
- The routine is idempotent: calling it twice in a row leaves the same end state and the second call throws
  nothing — tested
- `marketplace-common` `yarn test:cov` 100/4, `yarn test:mutation` 100, then `./deploy-local.sh` before any
  caller is committed (BCON-07)
**Traces:** NFR-SE01, NFR-SE03, NFR-SE05, NFR-MA01, NFR-MA02, NFR-MA05; ADR-003; BCON-01, BCON-07, BCON-08
**Evidence (gap):** no revocation routine exists in `BEs/marketplace-common/src/others/`

### E15-S05 — Changing a password ends every session   `not built`
**As a** customer who has just changed their password because I fear my account is compromised, **when** the
mutation succeeds, **I want** every session including the attacker's gone **so that** the change is the
remedy it appears to be.
**domains:** backend, database, testing
**Acceptance criteria:**
- Each of the three tiers' password-change mutations calls `revokeAllSessionsForAccount` after a **successful**
  Mongo write and never before it; a unit test per tier asserts the revoke is not called when the update
  reports `matchedCount === 0` or throws
- **The calling session is revoked with the rest** — no exemption, no re-issue: after a successful password
  change the caller's own next request is refused and it must log in again. A unit test per tier asserts the
  caller's session is among the revoked ones rather than leaving it emergent
- The caller's **access-token key is deleted too, not only its refresh key** — a revoke that drops the refresh
  key alone leaves the caller working until the access token expires naturally, which makes the decision
  above cosmetic for exactly the window an attacker is still inside. Tested by asserting both of the caller's
  keys are absent from Redis after the mutation returns
- The mutation's own failure mode is stated: if the revoke throws after the password write succeeded, the
  request fails loudly rather than returning success with sessions still live; tested by making the Redis
  double throw
- Per-service `yarn test:cov` 100/4 and `yarn test:mutation` 100
**Traces:** NFR-SE01, NFR-SE03, NFR-SE05, NFR-MA01, NFR-MA02, NFR-CO01; ADR-002, ADR-003; BCON-01, BCON-02
**Evidence (gap):** audit §3.3 — no credential mutation imports `redisClient`

> **Decided 2026-08-10: the calling session is revoked too.** The alternative was to exempt the caller and
> keep them signed in, which is friendlier and is what a fair number of products ship. It was rejected because
> it defeats the one scenario the feature exists for: the user changing a password because they believe
> someone else is inside the account cannot tell which of the live sessions is theirs, and neither can the
> server — an exemption is granted to *whichever session sent the mutation*, and an attacker who has the
> victim's password can send it. "Revoke all but me" is therefore not a weaker version of "revoke all"; it is
> a rule an attacker can aim at. Revoking everything makes the outcome the same no matter who calls it.
>
> **The exemption also cannot be built honestly here.** The routine is `revokeAllSessionsForAccount` — one
> input, `{ tier, accountId }`, four callers (E15-S04). Sparing the caller means a fifth parameter carrying a
> session identity into a routine whose whole point is that callers cannot get the shape wrong, plus a branch
> per call site that E17's operator-triggered revoke must then always pass as false. The uniform rule keeps
> the routine one-shaped and keeps its test surface finite.
>
> **What it costs, recorded so a later reversal is a decision rather than a discovery.** A password change is
> now a logout on every device, including the phone the user is holding. The three frontends must treat the
> refusal that follows as "log in again", not as an error state — no story in this epic owns that screen, and
> it is written here so it is not met for the first time in production. If the friction proves unacceptable,
> the reversal is a product decision that re-opens this question, not a patch to the routine.

### E15-S06 — Changing an email ends every session   `not built`
State plainly: the email address is the login identifier on all three tiers, so writing it is a credential
write and must tear sessions down exactly as a password change does.
**domains:** backend, database, testing
**Acceptance criteria:**
- Every mutation that writes an account's login email on any tier calls the same routine under the same
  success-only condition as E15-S05, with the same per-tier tests — including E15-S05's decided caller rule:
  the calling session goes with the rest, both of its keys, no exemption
- A grep-based test or lint rule enumerates the mutations that write a login-identifier field and asserts each
  one appears in the revoke call-site list, so a fourth such mutation added later cannot silently skip it
- The email-confirmation flow is examined and the finding recorded: whether a pending re-confirmation should
  also revoke, and whether the old address stays usable until confirmation
**Traces:** NFR-SE01, NFR-SE03, NFR-SE05, NFR-MA01, NFR-MA02; ADR-002, ADR-003; BCON-01, BCON-02
**Evidence (gap):** audit §3.3

### E15-S07 — Disabling or un-approving a ShopOwner ends their sessions   `not built`
**As a** platform operator, **when** I move a ShopOwner out of the approved state, **I want** their open
sessions to stop working immediately **so that** the status change is an actual control and not a label.
**domains:** backend, database, testing
**Acceptance criteria:**
- The Admin-side status mutation calls `revokeAllSessionsForAccount({ tier: 'ShopOwner', accountId })` after a
  successful write, gated on the existing `matchedCount` check at `funShopOwnerUpdateStatus.mts:39`
- One test per target status asserts revoke-or-not explicitly, including the transition **into** the approved
  state, which must **not** revoke
- An integration test seeds a live ShopOwner session, drives the status change, and asserts the next call to
  `marketplace-dev-authenticated-resource` is refused; every seeded key registered at creation and drained in
  `afterAll` (BCON-09)
**Traces:** NFR-SE01, NFR-SE03, NFR-SE05, NFR-MA01, NFR-MA02; ADR-002; BCON-01, BCON-02, BCON-09
**Evidence (gap):** `funShopOwnerUpdateStatus.mts:39` writes status and returns; no Redis import in that file

### E15-S08 — `waitApprov` is enforced, or the document stops claiming it is   `built 2026-08-13`
State plainly: `docs/devprotocol/phase3/SECURITY_AUTH.md:63` describes an enforcement that no code performs.
Exactly one of the two must change, and the epic must not leave both.
**domains:** backend, documentation, testing
**Acceptance criteria:**
- Either (a) the ShopOwner login path refuses an account in `waitApprov` with its own tested branch and its
  own boundary test, or (b) `SECURITY_AUTH.md:63` is corrected to describe what actually happens, with a note
  of why enforcement is not wanted
- Whichever branch is taken, `grep -rn 'waitApprov' docs/ BEs/` afterwards shows no document asserting an
  enforcement that no file implements — this is the mechanically checkable half
- If (a): an integration test proves a `waitApprov` ShopOwner cannot obtain a session, and E15-S07's
  transition tests cover the case where an already-logged-in owner is moved back into `waitApprov`
**Traces:** NFR-SE01, NFR-SE05, NFR-MA01, NFR-MA05, NFR-CO01; ADR-002; BCON-01, BCON-03
**Evidence (defect):** `docs/devprotocol/phase3/SECURITY_AUTH.md:63` versus the absent enforcement

**Evidence (built): branch (a), and this epic did not build it.** The story was written on 2026-08-10 against
a platform where nothing read the flag. **E01-S11 landed the gate on 2026-08-12**, from the other side of the
same problem — the approval queue was made real because E03-S08 opened self-registration, and a public form
that mints `waitApprov: true` is only safe if something refuses it. The two epics met in the middle.

What exists: `checkShopOwnerApproval` (`BEs/marketplace-common/src/others/checkShopOwnerApproval.mts`),
called by `tryLoginShopOwner` on 4028 **after** the password check — before it, the timing difference would
say which addresses belong to parked accounts — and by `tokenInfoShopOwner` on 4029 at every rotation, so
parking an owner bites within one access-token lifetime rather than one refresh-token lifetime. The failure
is the generic `throwUnauthorizedError` every other login failure returns. The flag is truthy-or-absent
(`funShopOwnerUpdateStatus` `$unset`s it), so `if (shopOwner.waitApprov)` is the whole check.

The second criterion is the mechanically checkable one and it is what this story actually did: after
E01-S11, the document asserting an enforcement no file implements was **this epic's own §2 and §3**, not
`SECURITY_AUTH.md` — whose v1.5 row at `:80` already read "Enforced since 2026-08-12" with both call sites
named. Both are corrected above; the audit's §3.6e is annotated at its source. `grep -rn 'waitApprov' docs/
BEs/` now leaves nothing claiming the gap.

⚠️ **The document was not "corrected" in the direction the story's option (b) imagined** — deleting the claim
would have replaced a true sentence with a false one. Option (b) was written when the code disagreed with
the doc; the code now agrees, so the only stale text left was the description of the disagreement.

**Decided 2026-08-13** (§6 question 2, closed): enforcement, and it does not reopen ADR-002. The gate reads
one account-state flag on one collection, in the two services that already authenticate that collection —
it is not a permission enum, nothing branches on a role name, and no fifth tier inherits anything from it.
CON-12 records the same conclusion from the lint side: the two authorization repos are the flag's legitimate
readers, and a rule refusing the read would be a rule refusing the gate.

### E15-S09 — The logout service's context type stops lying   `built 2026-08-12`
State plainly: the logout resolver's context is typed narrower than what it actually receives, and the guard
that compensates is unreachable — which is a mutation-testing problem as much as a typing one.
**domains:** backend, testing
**Acceptance criteria:**
- `logout.mts`'s context type is widened to the shape the middleware genuinely supplies, so the compensating
  runtime guard is no longer load-bearing for type-safety
- Any guard left in place as defence-in-depth that is now provably always true carries the repo's existing
  `// Stryker disable next-line` convention with a comment naming *why* it cannot be exercised — matching the
  precedent already used elsewhere in `BEs/`, rather than a threshold change (BCON-02: never lower a gate)
- `yarn test:mutation` reports 100 with no survived mutants and no new ignore beyond the annotated line
**Traces:** NFR-MA01, NFR-MA02, NFR-MA05; BCON-02
**Evidence (gap):** `BEs/dev/marketplace-dev-authenticated-logout/src/graphQLApi/schema/mutations/logout.mts`

**Evidence (built):** the widened type is `IContextLogoutResolver`, declared and exported beside the resolver
in `logout.mts` as `Omit<IContextLogout, 'state'> & { state: { user?: … } }`. It stays local by decision:
`IContextLogout` lives in the published `@axiumine/koa-utils` and is *correct* for the services whose
middleware always fills `state.user` — this is the one service where the middleware has an exit that does
not, so the narrowing is wrong here and nowhere else.

What the type was hiding was not one guard but three compensations, all now gone: `ctx.state.user?.accessToken`
(an optional chain placed *after* a dereference of the same object, so a mutant stripping it could not be
killed — hence the `// Stryker disable next-line OptionalChaining`), the `ctx.state.user.accessToken!`
non-null assertion the chain then forced, and the exclusion itself. The absent-session case is now an early
return the type demands and a test exercises. **No new exclusion was added — one was deleted.** The two that
remain in this repo are in `authorizationLogoutHandler.mts:59,68` and predate this story (E13-S11).

The behaviour a caller sees is unchanged — `x-introspectioncode` with no session still answers `true`,
deletes nothing and clears no cookie, which `test/integration/index.itest.mts` asserted before and after.
What changed is how: that `true` used to be a `TypeError` on `undefined.refreshToken`, caught by the
resolver's own `catch` and filed to Sentry as a failed session teardown. Every such call produced one. The
unit test pins all three halves — `resolves.toBe(true)`, `del` untouched, `captureException` untouched — and
the last of those is what kills the mutant that deletes the early return.

Gates in `marketplace-dev-authenticated-logout`: `yarn test:cov` 13 files / 127 tests, 100% on all four
metrics (Statements 120/120, Branches 46/46, Functions 27/27, Lines 116/116); `yarn test:mutation` **100.00,
107 killed, 0 survived** — five more live mutants than before the story, since removing the exclusion put
them back in scope; `yarn test:integration` 26 passed against the live Redis cluster.

## 5. Dependencies

- **Depends on E13-S01 (`hashSessionToken` and the hashed key namespace).** Not a preference — a hard
  ordering. The index stores session **key names**. Before E13 those key names *are* the raw tokens, so an
  index built first would concentrate every live credential on the platform into one greppable hash and make
  a Redis dump strictly more dangerous than it is today. After E13 the key name is a SHA-256 digest, useless
  without its preimage, and the index is safe to build. **E15-S01 is exempt** — the logout fix touches no key
  shape and should land first of everything in this backlog.
- **Depends on E14-S01** for the widened `IRefreshData`; both epics edit that type and the same login writers.
  Landing E14 first avoids editing them twice.
- **E17 depends on this epic** — the admin console reads this index and calls this revoke routine. It adds no
  new Redis shape of its own.
- **Landing order** (BCON-05, BCON-07):
  1. ~~`marketplace-dev-authenticated-logout` — E15-S01 and E15-S09, own branch, own parent-pointer bump.~~
     **Done 2026-08-12**, one branch and one pointer bump each. This repo is not touched again until step 5.
  2. `marketplace-common` — `revokeAllSessionsForAccount`, index write/prune helpers. Commit,
     `./deploy-local.sh`. **Partly done 2026-08-13**: `indexSession` and the key shape landed with E15-S02,
     `unindexSession`, the field TTL and `assertHashFieldTTLSupport` with E15-S03. Only the revoke routine
     (E15-S04) is still open, so this repo is entered once more.
  3. ~~`marketplace-dev-public-authorization` — index writes at login (all four writers).~~
     **Done 2026-08-13** with E15-S02: all four writers share one `setRedisLoginSession`, so the write is
     one call site, and rotation's is in `marketplace-common` alongside it.
  4. `marketplace-dev-authenticated-resource`, `-admin-authenticated-resource`,
     `-user-authenticated-resource` — credential-write call sites (E15-S05, E15-S06, E15-S07).
  5. ~~`marketplace-dev-authenticated-logout` — index prune on logout.~~ **Done 2026-08-13** with E15-S03:
     `unindexSession` after `deleteSession`. The service needs no version probe — `hDel` is a Redis 2.0
     command, and it issues nothing newer.
  6. ~~`docs/` — the key shape in `docs/data-model.md`.~~ **Done 2026-08-13** with E15-S02, which is when
     the shape became real; the row is `live` and carries the four load-bearing properties of the index.
     E15-S03 added the fifth — the per-field TTL — and the three ways a field leaves the index.
     ~~and `SECURITY_AUTH.md:63` for E15-S08~~ — that half is done too: E15-S08 closed on 2026-08-13
     against a gate E01-S11 had already built, and the only stale text was this epic's own.
  7. Parent workspace — one commit bumping every touched submodule pointer (ADR-031).

## 6. Open questions

Answered questions leave this table and become a decision on the story that implements them, with their
reasoning — this section holds only what is still undecided.

| # | Question | Owner |
|---|---|---|
| 3 | Should the email-confirmation flow revoke? Deferred to E15-S06's investigation output. | Product |

**Answered:**

- *What non-secret fields does an index entry carry?* — **`tier` and `mintedAt`, and nothing else**, decided
  2026-08-13 by the platform owner, carried by E15-S02. The field *name* is the session digest and already
  identifies the session; the value exists only so E17 can render a row without ever holding a token. `tier`
  is what lets a caller rebuild the session key from the digest; `mintedAt` is what makes the row readable to
  a human deciding which session to end. **One half of that turned out to be wrong once built** (E15-S02):
  the key to rebuild is `${REDIS_KEY}${field}` and needs no tier at all, because the tier is in the *index
  key's own name*. `tier` stays in the value on the second reason alone — a row E17 can render without
  parsing a key name — and the pair was agreed as a pair. **`familyId` and the cap were offered and refused**: both are E14
  lineage state that rotation already maintains on the session hash, and duplicating them here would mean two
  writers keeping one truth in step across every rotation. Nothing device- or network-derived is in the set,
  so the standing GDPR decision needs no exception for this key.
- *`waitApprov`: enforce, or correct the document?* — **enforce**, and it was already enforced: E01-S11 built
  the gate on 2026-08-12 while this question was open. Closed 2026-08-13 by E15-S08, which found the only
  document still claiming the gap was this epic. Reasoning on the story, including why it does not reopen
  ADR-002.
- *Does a password change revoke the calling session too?* — **yes**, decided 2026-08-10, carried by E15-S05
  with its reasoning and inherited by E15-S06.
- *Stale-field pruning: `HEXPIRE` or lazy prune?* — **`HEXPIRE`**, decided 2026-08-10, **built by E15-S03 on
  2026-08-13**. Redis 7.4 floor recorded in [`docker-DBs/README.md`](../../../../docker-DBs/README.md)
  §Redis, checked by `up.sh --with-redis` and by a boot probe in each of the four authorization services;
  **Valkey is not a supported target**, decided the same day.
